stages:
    - dev-build

mvn_build:
    stage: dev-build
    tags:
        - dev
        - maven
        - author
    script:
        - mvn clean install -Pauto-install-package -Dcq.url=http://10.234.4.249:4502
    only:
        - develop
        - master