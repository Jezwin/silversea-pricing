stages:
    - dev-build

mvn_build_dev_author:
    stage: dev-build
    tags:
        - dev
        - maven
        - author
    script:
        - mvn clean install -Pauto-install-package -Dcq.url=http://34.249.35.114
    only:
        - develop

mvn_build_dev_publish1:
    stage: dev-build
    tags:
        - dev
        - maven
        - author
    script:
        - mvn clean install -Pauto-install-package -Dcq.url=http://34.251.88.130
    only:
        - develop

mvn_build_dev_publish2:
    stage: dev-build
    tags:
        - dev
        - maven
        - author
    script:
        - mvn clean install -Pauto-install-package -Dcq.url=http://34.252.20.219
    only:
        - develop