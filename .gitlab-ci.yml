stages:
    - dev-build

mvn_build_dev_author:
    stage: dev-build
    tags:
        - dev
        - maven
        - author
    script:
        - mvn clean install -Pauto-install-package -Dcq.url=http://ssc-int-author.dmz.stde.sqli.com:4502
    only:
        - develop
        - master

mvn_build_dev_publish1:
    stage: dev-build
    tags:
        - dev
        - maven
        - author
    script:
        - mvn clean install -Pauto-install-package -Dcq.url=http://ssc-int-publish1.dmz.stde.sqli.com:4503
    only:
        - develop
        - master

mvn_build_dev_publish2:
    stage: dev-build
    tags:
        - dev
        - maven
        - author
    script:
        - mvn clean install -Pauto-install-package -Dcq.url=http://ssc-int-publish2.dmz.stde.sqli.com:4503
    only:
        - develop
        - master