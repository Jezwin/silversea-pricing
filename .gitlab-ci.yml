stages:
    - dev-build

mvn_build_dev_author:
    stage: dev-build
    tags:
        - dev
        - maven
        - author
    script:
        - mvn clean install -Pauto-install-package -Dcq.url=http://10.234.4.249:4502
    only:
        - develop
        - master

mvn_build_dev_publish1:
    stage: dev-build
    tags:
        - dev
        - maven
        - author
    script:
        - mvn clean install -Pauto-install-package -Dcq.url=http://10.234.4.250:4503
    only:
        - develop
        - master

mvn_build_dev_publish2:
    stage: dev-build
    tags:
        - dev
        - maven
        - author
    script:
        - mvn clean install -Pauto-install-package -Dcq.url=http://10.234.4.251:4503
    only:
        - develop
        - master